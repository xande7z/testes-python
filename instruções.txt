Testes unitários com Python
e o pacote 'unittest'

1) Criar o arquivo a ser testado chamado 'calculadora.py'

2) Criar o arquivo testes unitários chamado test.calculadora.py

3) Executar o arquivo de testes unitários
- comando no Terminal: python test-calculadora.py

4) Criar o arquivo a ser testado chamado 'calculadoraIMC.py'

    def testImc_normal(self):
        self.assertEqual(self.calc.calcular_IMC(60, 1.60), 'normal')
        
    def testImc_sobrepeso(self):
        self.assertEqual(self.calc.calcular_IMC(100, 1.60), 'sobrepeso')
        
    def testImc_obesidade(self):
        self.assertEqual(self.calc.calcular_IMC(180, 1.60), 'obesidade')
        